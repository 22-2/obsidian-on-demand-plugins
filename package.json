{
	"name": "on-demand-plugins",
	"version": "1.0.21",
	"description": "Lazy load plugins by caching their commands. Plugins are enabled on-demand when triggered, reducing startup time.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && pnpm run build:nocheck",
		"build:nocheck": "node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^25.2.0",
		"builtin-modules": "5.0.0",
		"esbuild": "0.27.2",
		"obsidian": "latest",
		"obsidian-typings": "^4.93.0",
		"tslib": "2.8.1",
		"typescript": "5.9.3"
	}
}
